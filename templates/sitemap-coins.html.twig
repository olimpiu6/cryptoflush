<?xml version="1.0" encoding="UTF-8"?>
    <urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    {% for url in url_list %}
        <url>
            <loc>{{ hostname~'coins/'~url.coin_ticker }}</loc>
            <lastmod>{{ "now"|date("Y-m-d") }}</lastmod>
        </url>
    {% endfor %}
    </urlset>